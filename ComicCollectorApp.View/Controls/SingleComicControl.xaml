<UserControl x:Class="ComicCollectorApp.View.Controls.SingleComicControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComicCollectorApp.View.Controls"
             mc:Ignorable="d" d:DesignHeight="150" d:DesignWidth="230">
    <UserControl.Resources>
        <ControlTemplate x:Key="validationFailed">
            <StackPanel Orientation="Horizontal">
                <Border BorderBrush="White" BorderThickness="2">
                    <AdornedElementPlaceholder />
                </Border>
            </StackPanel>
        </ControlTemplate>
        <Style x:Key="errorTextStyle" TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self},
                    Path=(Validation.Errors)[0].ErrorContent}" />
                    <Setter Property="Background" Value="LightPink" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Issue number:" Margin="3"/>
            <TextBox Margin="3" Text="{Binding Name, Mode=TwoWay,
                     UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource errorTextStyle}"
                     Validation.ErrorTemplate="{StaticResource validationFailed}"/>
            <TextBlock Text="Number print version:" Margin="3"/>
            <TextBox Margin="3" Text="{Binding PhoneNumber, Mode=TwoWay,
                     UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Style="{StaticResource errorTextStyle}"
                     Validation.ErrorTemplate="{StaticResource validationFailed}"/>
            <TextBlock Text="Is key issue:" Margin="3"/>
            <CheckBox Margin="3" IsChecked="{Binding Email, Mode=TwoWay,
                     UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     Validation.ErrorTemplate="{StaticResource validationFailed}"/>
        </StackPanel>
    </Grid>
</UserControl>
